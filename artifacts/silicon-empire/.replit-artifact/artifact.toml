kind = "web"
previewPath = "/"
title = "Silicon Empire"
version = "1.0.0"
id = "artifacts/silicon-empire"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21971

[services.development]
run = "pnpm --filter @workspace/silicon-empire run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/silicon-empire", "run", "build" ]
publicDir = "artifacts/silicon-empire/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21971"
BASE_PATH = "/"
